import styled from "styled-components";

export const ProductArea = styled.section`
    padding: 16px 32px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 32px;
    background: #dedede;
` 